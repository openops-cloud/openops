apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.analytics.name }}
  labels:
    app: {{ .Values.analytics.name }}
spec:
  type: ClusterIP
  ports:
  - port: 8088
    targetPort: 8088
    protocol: TCP
    name: http
  selector:
    app: {{ .Values.analytics.name }}