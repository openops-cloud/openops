---
title: "Release notes"
description: "What’s new in OpenOps."
icon: "🗞️"
iconType: "emoji"
---

## 2026-02-04

```json
{
  "✨": [
    "Analytics deployments now pull the latest published image version, updating Docker Compose and Helm defaults to analytics `0.14.2`.",
    "Chat UI styling is more consistent and readable, adding a `greyBlue-100` color token and refreshed Markdown typography.",
    "Autosend now waits for interactive UI tools to finish, reducing accidental early sends."
  ],
  "🐛": [
    "Passwords now accept the full intended set of special characters, including `[` and `]`.",
    "The Outlook new-email trigger now caps each fetch at 50 messages to reduce timeouts and over-fetching."
  ],
  "🚀": [
    "The AI model picker now shows provider-specific model names and icons in chat.",
    "Tool runs now capture and display results and errors more reliably."
  ]
}
```
