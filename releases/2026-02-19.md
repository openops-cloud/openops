---
title: "Release 0.100.0"
date: "2026-02-19"
version: "0.100.0"
---

# Release 0.100.0

- Date: 2026-02-19
- Version: 0.100.0

## Source data

```json
{
  "🆕 New Features": [
    "Added a Timeseries template table in OpenOps Tables for time-based records with a UUID primary key (`ID`) and fields like Date, Value, Workflow, Type, Description, Service, and Account.",
    "Added support for creating virtual analytics datasets from a SQL query via the optional non-empty `sql` field (PR #1958)."
  ],
  "🎁 Misc": [
    "Improved template table consistency by idempotently seeding the Timeseries template table at server startup using a FlagEntity marker.",
    "Improved socket resiliency by adding `reconnectSocket` to the UI-kit socket context to force a disconnect/connect cycle when needed.",
    "Fixed a corrupted `package-lock.json` entry for `@rollup/rollup-linux-arm-gnueabihf` by correcting the version to `4.20.0` and restoring metadata (PR #1966).",
    "Updated `axios` from 1.12.1 to 1.13.5 to include upstream security fixes (PR #1935).",
    "Updated `webpack` from 5.94.0 to 5.105.2 and refreshed the lockfile and third-party licenses (PR #1948).",
    "Updated `markdown-it` to 14.1.1 and refreshed the lockfile and third-party license metadata (PR #1950).",
    "Updated GitHub Actions versions used by CI and release workflows, including `aws-actions/configure-aws-credentials` v6.0.0 (PR #1953).",
    "Updated the analytics service image from 0.14.4 to 0.14.5 in Docker Compose and Helm configurations."
  ]
}

```
