---
title: Release 0.100.0
date: 2026-02-19
version: 0.100.0
---

# Release 0.100.0 (2026-02-19)

## 🆕 New Features

- Added support for creating analytics datasets as **virtual datasets** backed by a SQL query.

## 🐛 Bug Fixes

- Fixed no user-facing bugs in this release.

## 🎁 Misc

- Ensured the built-in timeseries template table exists at startup to prevent missing-table errors.
- Improved real-time update recovery after dropped connections by using a shared `reconnectSocket` routine.
- Added a `recreateIfExists` option for analytics dataset creation to replace existing datasets in one step.
- Updated Docker Compose and Helm to use analytics service version `0.14.5` (from `0.14.4`).
- Fixed the `package-lock.json` entry for `@rollup/rollup-linux-arm-gnueabihf` to `4.20.0` to prevent installs from pulling an invalid version.
- Updated `axios` to `1.13.5` and refreshed the lockfile and third-party licenses (#1935).
- Updated `webpack` to `5.105.2` and refreshed the lockfile and license list (PR #1948).
- Updated `markdown-it` to `14.1.1` and refreshed the lockfile and third-party license metadata.
- Updated GitHub Actions dependencies for `chromaui/action`, `aws-actions/configure-aws-credentials`, and `docker/build-push-action` (PR #1953).
