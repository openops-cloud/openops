---
title: "Release 0.100.0: analytics dataset updates"
date: 2026-02-19
version: 0.100.0
---

# Release 0.100.0 (2026-02-19)

## 🆕 New Features

- Added support for creating analytics datasets as **virtual datasets** backed by a SQL query.

## 🐛 Bug Fixes

- Fixed no user-facing bugs in this release.

## 🎁 Misc

- Ensured the built-in time series template table exists at startup to prevent missing-table errors.
- Improved real-time update recovery after dropped connections.
- Added an option to recreate an existing analytics dataset during creation.
- Updated Docker Compose and Helm to use analytics service version `0.14.5` (from `0.14.4`).
- Fixed an install issue caused by an invalid Rollup package version in `package-lock.json`.
- Updated `axios` to `1.13.5` and refreshed `package-lock.json` and the third-party license list (#1935).
- Updated `webpack` to `5.105.2` and refreshed `package-lock.json` and the third-party license list (PR #1948).
- Updated `markdown-it` to `14.1.1` and refreshed `package-lock.json` and the third-party license list.
- Updated GitHub Actions dependencies for `chromaui/action`, `aws-actions/configure-aws-credentials`, and `docker/build-push-action` (PR #1953).
