# What's new (2026-02-11)

- **Date:** 2026-02-11
- **Version:** 0.100.0

## 🆕 New Features

```json
{
  "🆕 New Features": [
    "Benchmark results can now be saved so they can be reviewed and queried later.",
    "User verification can now be performed against multiple allowed public keys.",
    "Outlook email attachments now support inline-related metadata so embedded content renders correctly in messages.",
    "Telemetry events now include the environment name to make it easier to segment and troubleshoot behavior across deployments."
  ],
  "🎁 Misc": [
    "Automated tests now produce less noise in GitHub CI logs, making failures easier to spot.",
    "The webhooks sync endpoint is now less likely to fail during long-running operations.",
    "Azure authentication now rejects invalid configurations earlier with clearer failures.",
    "Benchmark API contracts are now centralized to reduce duplication across packages.",
    "CI workflow dependencies were refreshed to keep automation up to date."
  ],
  "🐜 Bug Fixes": [
    "The Test Run Limits form now prevents invalid values and behaves more predictably when numeric fields are cleared.",
    "Approval and disapproval links now open through the proper resume page instead of exposing raw backend links."
  ]
}

```

## 🐛 Bug Fixes

```json
{
  "🆕 New Features": [
    "Benchmark results can now be saved so they can be reviewed and queried later.",
    "User verification can now be performed against multiple allowed public keys.",
    "Outlook email attachments now support inline-related metadata so embedded content renders correctly in messages.",
    "Telemetry events now include the environment name to make it easier to segment and troubleshoot behavior across deployments."
  ],
  "🎁 Misc": [
    "Automated tests now produce less noise in GitHub CI logs, making failures easier to spot.",
    "The webhooks sync endpoint is now less likely to fail during long-running operations.",
    "Azure authentication now rejects invalid configurations earlier with clearer failures.",
    "Benchmark API contracts are now centralized to reduce duplication across packages.",
    "CI workflow dependencies were refreshed to keep automation up to date."
  ],
  "🐜 Bug Fixes": [
    "The Test Run Limits form now prevents invalid values and behaves more predictably when numeric fields are cleared.",
    "Approval and disapproval links now open through the proper resume page instead of exposing raw backend links."
  ]
}

```

## 🎁 Misc

```json
{
  "🆕 New Features": [
    "Benchmark results can now be saved so they can be reviewed and queried later.",
    "User verification can now be performed against multiple allowed public keys.",
    "Outlook email attachments now support inline-related metadata so embedded content renders correctly in messages.",
    "Telemetry events now include the environment name to make it easier to segment and troubleshoot behavior across deployments."
  ],
  "🎁 Misc": [
    "Automated tests now produce less noise in GitHub CI logs, making failures easier to spot.",
    "The webhooks sync endpoint is now less likely to fail during long-running operations.",
    "Azure authentication now rejects invalid configurations earlier with clearer failures.",
    "Benchmark API contracts are now centralized to reduce duplication across packages.",
    "CI workflow dependencies were refreshed to keep automation up to date."
  ],
  "🐜 Bug Fixes": [
    "The Test Run Limits form now prevents invalid values and behaves more predictably when numeric fields are cleared.",
    "Approval and disapproval links now open through the proper resume page instead of exposing raw backend links."
  ]
}

```
