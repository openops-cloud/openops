# OpenOps release notes (0.100.0)

Date: 2026-02-11

## 🆕 New Features

- Added the ability to save benchmark results so you can review and query them later (PR #1916)
- Improved benchmark integrations by standardizing shared benchmark API types across services (PR #1921)
- Improved Azure authentication by validating configuration up front to surface problems earlier (PR #1923)
- Added attachment support to the Microsoft Outlook **send-email** action (PR #1927)

## 🐛 Bug Fixes

- Fixed cloud template signature verification to support multiple configured public keys (PR #1919)
- Fixed validation in the Test Run Limits dialog (PR #1918)

## 🎁 Misc

- Improved webhook sync reliability by increasing the NGINX proxy timeout for `/api/v1/webhooks/*/sync` (PR #1913)
- Reduced GitHub CI test log noise by running Jest with `--silent` via Nx (PR #1893)
- Updated GitHub Actions workflows to newer versions of shared actions like `actions/checkout`, `actions/cache`, and `chromaui/action` (PR #1905)
- Improved approval and disapproval links by opening the frontend resume page instead of a raw backend resume URL (PR #1922)
- Added the environment name to telemetry events to make filtering across deployments easier (PR #1926)